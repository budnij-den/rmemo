<%= form_with(model: meter, local: true) do |form| %>
  <% if meter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meter.errors.count, "error") %> prohibited this meter from being saved:</h2>

      <ul>
      <% meter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label :value_total %>
  <%= form.text_field :value_total %>
  <br>
  <%= form.label :placed_at %>
  <%= form.text_field :placed_at %>
  <br>
  <%= form.label :feature %>
  <%= form.text_field :feature %>
  <div class="actions">
    <%= form.submit class: 'btn btn-lg btn-primary' %>
  </div>
<% end %>

